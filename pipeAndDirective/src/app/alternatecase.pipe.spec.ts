import { AlternatecasePipe } from './alternatecase.pipe';

describe('AlternatecasePipe', () => {
  it('create an instance', () => {
    const pipe = new AlternatecasePipe();
    expect(pipe).toBeTruthy();
  });
});
